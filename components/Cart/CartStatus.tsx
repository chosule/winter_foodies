import { NaviUI } from "../layouts/BottomNavigation/style";

const CartStatus = () => {
  return (
    <>
      <NaviUI.CartQuantity>숫자표시</NaviUI.CartQuantity>
      {/* {products && <p>{product.수량}</p>} */}
    </>
  );
};

export default CartStatus;
